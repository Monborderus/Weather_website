const select = document.body.querySelector(".select-city");
const button = document.body.querySelector(".button");
const cityNameSpan = document.body.querySelector(".city-name-span");
const countryFlag =document.body.querySelector(".country-flag");
const mainImg = document.body.querySelector(".main-img");
const mainImgDescription = document.body.querySelector(".main-img-description");
const wind = document.body.querySelector(".wind");
const windSpeed = document.body.querySelector(".wind-speed");
const windSpeedValue = document.body.querySelector(".wind-speed-value");
const windDirection = document.body.querySelector(".wind-direction");
const windDirectionValue = document.body.querySelector(".wind-direction-value");
const windDirectionArrow = document.body.querySelector(".wind-direction-arrow");

const temp = document.body.querySelector(".temp");
const tempOriginal = document.body.querySelector(".temp-original");
const tempOriginalValue = document.body.querySelector(".temp-original-value");
const tempFeelsLike = document.body.querySelector(".temp-feels-like");
const tempFeelsLikeValue = document.body.querySelector(".temp-feels-like-value");

const additional = document.body.querySelector(".additional");
const additionalHumidity = document.body.querySelector(".additional-humidity");
const additionalHumidityValue = document.body.querySelector(".additional-humidity-value");
const additionalPressure = document.body.querySelector(".additional-pressure");
const additionalPressureVlaue = document.body.querySelector(".additional-pressure-value");

export {
  select,
  button,
  cityNameSpan,
  countryFlag,
  mainImg,
  mainImgDescription,
  wind,
  windSpeed,
  windSpeedValue,
  windDirection,
  windDirectionValue,
  windDirectionArrow,
  temp,
  tempOriginal,
  tempOriginalValue,
  tempFeelsLike,
  tempFeelsLikeValue,
  additional,
  additionalHumidity,
  additionalHumidityValue,
  additionalPressure,
  additionalPressureVlaue,
};
